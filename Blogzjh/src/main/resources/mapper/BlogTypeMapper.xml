<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.zjh.blog.dao.BlogTypeMapper" >

    <resultMap id="BaseResultMap" type="com.zjh.blog.domain.BlogType" >
        <id column="id" property="id" jdbcType="INTEGER" />
        <result column="type_name" property="typeName" jdbcType="VARCHAR" />
        <result column="order_num" property="orderNum" jdbcType="INTEGER" />
    </resultMap>

    <sql id="Base_Column_List" >
        id, type_name, order_num
    </sql>

    <select id="selectById" resultType="com.zjh.blog.domain.BlogType" parameterType="java.lang.Integer" >
        select
        <include refid="Base_Column_List" />
        from t_blogtype
        where id = #{id,jdbcType=INTEGER}
    </select>

    <delete id="delete" parameterType="java.lang.Integer" >
        delete from t_blogtype
        where id = #{id,jdbcType=INTEGER}
    </delete>

    <insert id="insert" parameterType="com.zjh.blog.domain.BlogType" >
        insert into t_blogtype (id, type_name, order_num
            )
        values (#{id,jdbcType=INTEGER}, #{typeName,jdbcType=VARCHAR}, #{orderNum,jdbcType=INTEGER}
            )
    </insert>

    <insert id="insertSelective" parameterType="com.zjh.blog.domain.BlogType" >
        insert into t_blogtype(type_name,order_num) values
        <trim prefix="values (" suffix=")" suffixOverrides="," >
            <if test="id != null" >
                #{id,jdbcType=INTEGER},
            </if>
            <if test="typeName != null" >
                #{typeName,jdbcType=VARCHAR},
            </if>
            <if test="orderNum != null" >
                #{orderNum,jdbcType=INTEGER},
            </if>
        </trim>
    </insert>

    <update id="update" parameterType="com.zjh.blog.domain.BlogType" >
        update t_blogtype
        <set >
            <if test="typeName != null" >
                type_name = #{typeName,jdbcType=VARCHAR},
            </if>
            <if test="orderNum != null" >
                order_num = #{orderNum,jdbcType=INTEGER},
            </if>
        </set>
        where id = #{id,jdbcType=INTEGER}
    </update>

</mapper>